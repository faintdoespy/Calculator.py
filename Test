```js
const { Client,GateWayIntentBits} = require('discord.js-selfbot-v13');

const client = new Client({
    checkUpdate: false,
});

client.on('ready', () => {
    console.log(`Logged in as ${client.user.tag}!`);
});

client.on('messageCreate', message => {
  if(message.channel.id === '814958381985955880') { //idk if channel id has to be a string or number
    console.log(message)
  }
});

client.login('caizyselfbot');```
